name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install release tooling
        run: python -m pip install '.[release]'

      - name: Derive release version from tag
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          case "$GITHUB_REF_NAME" in
            v?*)
              echo "RELEASE_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
              ;;
            *)
              echo "error: expected a tag like v1.2.3, got $GITHUB_REF_NAME" >&2
              exit 1
              ;;
          esac

      - name: Build and check distributions
        run: ./scripts/publish_pypi.sh --dry-run --yes --dist-dir dist --version "$RELEASE_VERSION"

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  macos:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Core unit tests
        run: swift test --package-path executor-apps/macos

      - name: Derive release version from tag
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          case "$GITHUB_REF_NAME" in
            v?*)
              echo "RELEASE_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
              ;;
            *)
              echo "error: expected a tag like v1.2.3, got $GITHUB_REF_NAME" >&2
              exit 1
              ;;
          esac

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Build DMGs (arm64 + x86_64)
        run: |
          set -euo pipefail
          export NEWBRO_APP_VERSION="$RELEASE_VERSION"
          mkdir -p executor-apps/macos/release
          for ARCH in arm64 x86_64; do
            echo "::group::build $ARCH"
            NEWBRO_APP_ARCH="$ARCH" ./executor-apps/macos/package-app.sh
            STAGE="$(mktemp -d)"
            cp -R "executor-apps/macos/dist/Newbro Executor.app" "$STAGE/"
            DMG="executor-apps/macos/release/NewbroExecutor-${NEWBRO_APP_VERSION}-${ARCH}.dmg"
            rm -f "$DMG"
            create-dmg \
              --volname "Newbro Executor ${NEWBRO_APP_VERSION}" \
              --window-size 540 380 \
              --icon-size 128 \
              --icon "Newbro Executor.app" 140 190 \
              --app-drop-link 400 190 \
              --no-internet-enable \
              "$DMG" \
              "$STAGE"
            rm -rf "$STAGE"
            echo "::endgroup::"
          done
          ls -la executor-apps/macos/release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: executor-apps/macos/release/*.dmg
          fail_on_unmatched_files: true
          body: |
            ## Newbro Executor (macOS)

            Unsigned menu-bar app. On first launch, right-click the app in
            `/Applications` and choose **Open** (or run
            `xattr -dr com.apple.quarantine "/Applications/Newbro Executor.app"`).

            **Downloads** (pick the asset matching your Mac)
            - Apple Silicon (M-series): the `-arm64.dmg` asset below
            - Intel: the `-x86_64.dmg` asset below
